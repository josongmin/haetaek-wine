# React App Configuration
REACT_APP_API_URL=http://localhost:4000

# Note: API keys should NOT be used in client-side code
# They will be exposed in the browser bundle
# Use backend proxy for sensitive API calls
